{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/brianterry/grip-protocol/spec/schemas/service/session-request.json",
  "title": "GRIP Service — Session Request",
  "description": "HTTP request body for POST /v1/sessions. Establishes verification context for subsequent verify requests.",
  "type": "object",
  "properties": {
    "protocolVersion": {
      "type": "string",
      "description": "GRIP protocol version the client supports.",
      "default": "0.3"
    },
    "frameworks": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Compliance frameworks to activate for this session. An empty array means all available checks are loaded.",
      "default": [],
      "examples": [["hipaa-security", "cis-aws-benchmark-level-1", "pci-dss"]]
    }
  },
  "required": ["protocolVersion"],
  "additionalProperties": false
}
